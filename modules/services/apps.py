from django.apps import AppConfig


class ServicesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "modules.services"
    label = "services"
    verbose_name = "Servicios y programación"
